/** Single queue consumed by the Render worker. */
export const SCRAPE_QUEUE_NAME = 'scrape';

/** One job name for discount scrape → Sale pipeline. */
export const SCRAPE_JOB_NAME = 'discount-scrape';
